# Telegram Mini App - Документация

## Описание

Telegram Mini App (Web App) для платформы АТЦ позволяет представителям организаций получать доступ к информации о слушателях, расписанию и сертификатам прямо в Telegram через современный веб-интерфейс.

## Возможности

### ✅ Для представителей организаций:

1. **Регистрация через Telegram**
   - Многошаговая форма регистрации
   - Автозаполнение данных из профиля Telegram
   - Выбор организации из списка или ввод вручную

2. **Просмотр слушателей**
   - Список всех слушателей организации
   - Фильтрация по курсам
   - Поиск по имени
   - Группировка по учебным группам

3. **Расписание занятий**
   - Актуальное расписание на 60 дней вперёд
   - Группировка по датам
   - Информация о типе занятия, преподавателе и аудитории
   - Красивые индикаторы типов занятий

4. **Сертификаты**
   - Список выданных сертификатов
   - Фильтрация по периодам
   - Информация о прохождении обучения и посещаемости
   - Скачивание PDF-файлов сертификатов

5. **Настройки**
   - Просмотр профиля
   - Информация о правах доступа
   - О приложении

---

## 🚀 Локальное тестирование (без домена)

### Проблема

Telegram Mini App требует публичный HTTPS URL для работы. Localhost не подходит, так как Telegram не может обращаться к вашему компьютеру напрямую.

### Решение: ngrok

**ngrok** создаёт безопасный туннель к вашему localhost, предоставляя публичный URL.

#### Шаг 1: Установка ngrok

```bash
# macOS (через Homebrew)
brew install ngrok/ngrok/ngrok

# Или скачайте с сайта
# https://ngrok.com/download
```

#### Шаг 2: Регистрация и получение токена

1. Зарегистрируйтесь на [ngrok.com](https://ngrok.com)
2. Получите Auth Token на странице [dashboard](https://dashboard.ngrok.com/get-started/your-authtoken)
3. Добавьте токен:
   ```bash
   ngrok config add-authtoken YOUR_TOKEN_HERE
   ```

#### Шаг 3: Запуск приложения локально

```bash
# Запускаем приложение Nuxt
npm run dev
# Приложение будет доступно на http://localhost:3000
```

#### Шаг 4: Создание туннеля с ngrok

В новом терминале:

```bash
# Создаём туннель к порту 3000
npm run ngrok

# Или напрямую:
ngrok http 3000
```

Вы получите вывод вида:

```
Session Status                online
Account                       your@email.com
Version                       3.x.x
Region                        Europe (eu)
Latency                       -
Web Interface                 http://127.0.0.1:4040
Forwarding                    https://abc123.ngrok-free.app -> http://localhost:3000

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

**Важно!** Скопируйте HTTPS URL (например, `https://abc123.ngrok-free.app`)

#### Шаг 5: Настройка Web App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Нажмите **Menu Button** → **Edit Menu Button**  
5. Нажмите **Configure Web App**
6. Введите URL (используйте ngrok URL):
   ```
   https://abc123.ngrok-free.app/tg-app
   ```
7. Введите название кнопки: **"Открыть платформу"**

#### Шаг 6: Тестирование

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку меню (≡) справа от поля ввода
3. Выберите **"Открыть платформу"**
4. Mini App должен открыться!

### Важные моменты при локальном тестировании

⚠️ **Каждый раз при перезапуске ngrok URL меняется!**  
Если вы перезапустили ngrok, нужно обновить URL в BotFather.

#### Фиксированный поддомен (платная версия ngrok)

С платной подпиской ngrok можно использовать фиксированный URL:

```bash
ngrok http 3000 --subdomain=my-atc-platform
# URL будет: https://my-atc-platform.ngrok.io
```

#### Альтернативы ngrok

- **localtunnel** (бесплатно, но менее стабильно)
  ```bash
  npx localtunnel --port 3000
  ```

- **cloudflared** (от Cloudflare, бесплатно)
  ```bash
  cloudflared tunnel --url http://localhost:3000
  ```

### Режим разработки (DEV MODE)

Приложение поддерживает режим разработки для тестирования без реального Telegram окружения.

#### Как работает

Когда вы открываете `/tg-app` в обычном браузере (не в Telegram):
- Автоматически определяется отсутствие Telegram WebApp
- Используются mock данные для тестирования
- Chat ID: `123456789`
- Имя: `Dev User`

#### Настройка для тестирования

1. **Создайте тестового представителя** в админ панели:
   - Telegram Chat ID: `123456789`
   - Telegram Username: `devuser`
   - Статус: `approved`
   - Настройте необходимые права доступа

2. **Откройте в браузере**:
   ```
   https://your-ngrok-domain.ngrok-free.dev/tg-app
   ```

3. **Проверьте консоль**:
   ```
   [DEV MODE] Telegram WebApp недоступен, используем mock данные
   [DEV MODE] Используем mock chatId для тестирования
   ```

#### Важно

⚠️ **Режим разработки работает только в `NODE_ENV=development`**  
В продакшене mock данные не используются!

#### Изменить тестовый Chat ID

Если нужен другой ID для тестирования, измените в:

1. `app/pages/tg-app/index.vue` (строка ~192)
2. `server/api/tg-app/auth.post.ts` (строка ~26)

### Исправление ошибок HMR (Hot Module Replacement)

Если видите ошибку `Mixed Content` в консоли:

```
Mixed Content: The page at 'https://...' was loaded over HTTPS, 
but attempted to connect to the insecure WebSocket endpoint 'ws://...'
```

**Решение:**

1. Убедитесь, что в `.env` указан ngrok домен:
   ```bash
   NGROK_DOMAIN=your-domain.ngrok-free.dev
   ```

2. Перезапустите dev сервер:
   ```bash
   # Ctrl+C для остановки
   npm run dev
   ```

3. Очистите кэш браузера (Ctrl+Shift+R)

Приложение автоматически использует WSS (защищенный WebSocket) для HMR через ngrok.


### Отладка в режиме разработки

#### 1. Просмотр логов

```bash
# Логи Nuxt приложения
# Смотрите в терминале где запущен npm run dev

# Логи ngrok (HTTP трафик)
# Откройте в браузере: http://localhost:4040
```

#### 2. DevTools в Telegram Desktop

Telegram Desktop позволяет открыть DevTools для Mini App:

1. Откройте Mini App в Telegram Desktop
2. Правой кнопкой мыши → **Inspect Element**
3. Появятся Chrome DevTools

#### 3. Мобильная отладка

Для Android используйте Chrome Remote Debugging:

1. Подключите телефон через USB
2. Включите отладку по USB
3. Откройте `chrome://inspect` в Chrome на компьютере
4. Откройте Mini App в Telegram на телефоне
5. Найдите WebView в списке устройств

### Проверка работы API

Тестируйте API эндпоинты напрямую:

```bash
# Проверка эндпоинта студентов
curl "https://abc123.ngrok-free.app/api/tg-app/students?organizationId=YOUR_ORG_ID"

# Проверка авторизации (нужен initData из Telegram)
curl -X POST "https://abc123.ngrok-free.app/api/tg-app/auth" \
  -H "Content-Type: application/json" \
  -d '{"initData": "..."}'
```

---

## Настройка Telegram Mini App (продакшн)

### 1. Создание Mini App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Нажмите **Menu Button** → **Edit Menu Button**
5. Нажмите **Configure Web App**
6. Введите URL вашего приложения:
   ```
   https://ваш-домен.com/tg-app
   ```
7. Введите название кнопки (например, "Открыть платформу")

### 2. Настройка переменных окружения

В файле `.env` убедитесь, что указаны:

```env
# Telegram Bot Token
TELEGRAM_BOT_TOKEN=your_bot_token_here

# URL приложения (для правильной работы Web App)
NUXT_PUBLIC_APP_URL=https://ваш-домен.com
```

### 3. Проверка работы

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку меню (три полоски справа от поля ввода)
3. Выберите **"Открыть платформу"** (или как вы назвали)
4. Telegram Mini App должен открыться

---

## Технические детали

### Архитектура

```
/app/pages/tg-app/index.vue          - Главная страница Mini App
/app/components/tg-app/
  ├── RegistrationForm.vue            - Форма регистрации
  ├── StudentsTab.vue                 - Вкладка слушателей
  ├── ScheduleTab.vue                 - Вкладка расписания
  ├── CertificatesTab.vue             - Вкладка сертификатов
  └── SettingsTab.vue                 - Вкладка настроек

/server/api/tg-app/
  ├── auth.post.ts                    - Авторизация по initData
  ├── register.post.ts                - Регистрация представителя
  ├── students.get.ts                 - Получение слушателей
  ├── schedule.get.ts                 - Получение расписания
  └── certificates.get.ts             - Получение сертификатов
```

### Авторизация

Mini App использует `window.Telegram.WebApp.initData` для авторизации:

1. При загрузке приложения получается `initData`
2. Данные отправляются на `/api/tg-app/auth`
3. Сервер парсит `initData` и проверяет существование представителя
4. Если представитель найден, возвращаются его данные
5. Если нет - показывается форма регистрации

### Состояния представителя

- **`pending`** - Заявка на рассмотрении (доступ к данным ограничен)
- **`approved`** - Одобрен (полный доступ согласно правам)
- **`blocked`** - Заблокирован (доступ запрещён)

### Права доступа

Каждый представитель имеет набор прав:

```typescript
{
  can_view_students: boolean;      // Просмотр слушателей
  can_view_schedule: boolean;      // Просмотр расписания
  can_view_certificates: boolean;  // Просмотр сертификатов
}
```

Права настраиваются администратором в веб-интерфейсе платформы.

---

## Дизайн и UX

### Цветовая схема

- **Фон:** Тёмный градиент (#0f172a → #1e293b)
- **Акцент:** Синий → Фиолетовый градиент (#3b82f6 → #8b5cf6)
- **Текст:** Светлый (#f1f5f9)
- **Вторичный текст:** Серый (#94a3b8)

### Анимации

- Плавные переходы между вкладками
- Коллапс/раскрытие групп
- Индикаторы загрузки
- Haptic feedback при взаимодействии

### Адаптивность

- Оптимизировано для мобильных устройств
- Работает в полноэкранном режиме Telegram
- Липкая навигация снизу экрана
- Корректное отображение на всех размерах экрана

---

## API Endpoints

### POST `/api/tg-app/auth`

Авторизация представителя по `initData`.

**Request:**
```json
{
  "initData": "query_id=...&user=...&auth_date=..."
}
```

**Response (успех):**
```json
{
  "success": true,
  "representative": {
    "id": "uuid",
    "fullName": "Иванов Иван Иванович",
    "phone": "+998901234567",
    "organizationId": "uuid",
    "organization": { "id": "uuid", "name": "ООО Компания" },
    "status": "approved",
    "permissions": { ... }
  }
}
```

**Response (не найден):**
```json
{
  "statusCode": 404,
  "message": "Пользователь не найден"
}
```

---

### POST `/api/tg-app/register`

Регистрация нового представителя.

**Request:**
```json
{
  "initData": "query_id=...&user=...&auth_date=...",
  "fullName": "Иванов Иван Иванович",
  "phone": "+998901234567",
  "organizationId": "uuid" // или null
  "organizationName": "ООО Компания", // если organizationId = null
  "username": "ivan_ivanov" // опционально
}
```

**Response:**
```json
{
  "success": true,
  "message": "Заявка успешно отправлена",
  "representative": { ... }
}
```

---

### GET `/api/tg-app/students?organizationId=uuid`

Получение списка слушателей организации.

**Response:**
```json
{
  "success": true,
  "students": [
    {
      "fullName": "Петров Петр Петрович",
      "groupName": "Группа 2024-01",
      "courseName": "Курс безопасности",
      "startDate": "01.01.2024",
      "endDate": "30.06.2024"
    }
  ]
}
```

---

### GET `/api/tg-app/schedule?organizationId=uuid`

Получение расписания занятий.

**Response:**
```json
{
  "success": true,
  "schedule": [
    {
      "date": "2024-12-25",
      "startTime": "09:00",
      "endTime": "11:00",
      "eventType": "theory",
      "disciplineName": "Основы безопасности",
      "instructorName": "Смирнов С.С.",
      "location": "Аудитория 101",
      "groupName": "Группа 2024-01"
    }
  ]
}
```

---

### GET `/api/tg-app/certificates?organizationId=uuid`

Получение сертификатов слушателей.

**Response:**
```json
{
  "success": true,
  "certificates": [
    {
      "id": "uuid",
      "studentName": "Петров Петр Петрович",
      "certificateNumber": "АТЦ-2024-001",
      "courseName": "Курс безопасности",
      "groupCode": "2024-01",
      "issueDate": "30.06.2024",
      "status": "issued",
      "pdfFileUrl": "/uploads/certificates/...",
      "hasPassed": true,
      "attendancePercent": 95.5
    }
  ]
}
```

---

## Разработка

### Локальное тестирование

Для тестирования Mini App локально:

1. **Используйте ngrok** для создания публичного URL:
   ```bash
   npm run ngrok
   ```

2. **Настройте Web App в BotFather** с ngrok URL:
   ```
   https://your-domain.ngrok.io/tg-app
   ```

3. **Откройте бота в Telegram** и протестируйте

### Отладка

- Используйте Telegram Desktop для доступа к DevTools
- В мобильном приложении Telegram включите режим разработчика
- Все ошибки логируются в консоль браузера

---

## Сравнение с Telegram Bot

| Функция                    | Telegram Bot | Mini App |
|---------------------------|-------------|----------|
| Регистрация               | ✅ FSM       | ✅ Форма  |
| Список слушателей         | ✅ Текст     | ✅ UI     |
| Расписание                | ✅ Текст     | ✅ UI     |
| Сертификаты               | ✅ Текст     | ✅ UI     |
| Фильтрация данных         | ✅ Кнопки    | ✅ UI     |
| Скачивание сертификатов   | ✅           | ✅        |
| Визуальный интерфейс      | ❌           | ✅        |
| Работает offline          | ❌           | ❌        |

---

## Безопасность

1. **Валидация initData** - сервер проверяет подлинность данных от Telegram
2. **Проверка прав доступа** - каждый endpoint проверяет права представителя
3. **Ограничение данных** - представитель видит только данные своей организации
4. **Rate limiting** - защита от злоупотребления API

---

## Поддержка

Для вопросов и предложений обращайтесь к разработчикам платформы.

© 2026 АТЦ Платформа. Все права защищены.
