# 🚀 Чеклист подготовки к деплою

## ✅ Выполнено

### 1. Очистка проекта
- [x] Удалена папка `docs/` со всей документацией
- [x] Удалены файлы отчетов в корне проекта:
  - IMPLEMENTATION_REPORT.md
  - TELEGRAM_BOT_AUDIT_REPORT.md
  - TELEGRAM_BOT_FINAL_REPORT.md
  - TELEGRAM_BOT_OPTIMIZATIONS_IMPLEMENTED.md
  - TELEGRAM_BOT_OPTIMIZATION_PLAN.md
- [x] Удалена папка `debug/` с тестовыми скриптами
- [x] Удален файл `check-enum.ts`
- [x] Удален архив `atc_platform.zip`
- [x] Обновлен `.gitignore` для игнорирования:
  - debug файлов
  - документации
  - архивов
  - служебных папок (.agent, .gemini)
- [x] Обновлен `README.md` (удалены ссылки на документацию)

### 2. Конфигурация
- [x] `package.json` - все зависимости актуальны
- [x] `nuxt.config.ts` - настроен preset для Netlify
- [x] `netlify.toml` - корректная конфигурация для деплоя
- [x] `.env.example` - актуальный шаблон переменных окружения

## ⚠️ Требует внимания

### 3. Безопасность и оптимизация

#### 3.1. Логирование
✅ **Созданы утилиты для правильного логирования:**
- `server/utils/logger.ts` - для серверной части
- `app/utils/logger.ts` - для клиентской части

**Преимущества:**
- Автоматическое отключение debug-логов в production
- Централизованное управление логированием
- Структурированные логи с префиксами
- Поддержка разных уровней логирования

**Использование:**
```typescript
// Серверная часть
import { logger } from '~/server/utils/logger'

logger.info('Информация')        // Показывается всегда
logger.warn('Предупреждение')    // Показывается всегда
logger.error('Ошибка', error)    // Показывается всегда
logger.debug('Отладка')          // Только в development
logger.success('Успех')          // Только в development

// Клиентская часть
import { logger } from '~/utils/logger'

logger.info('Информация')
logger.debug('Отладка')          // Только в development
logger.ui('click', 'button')     // Только в development
logger.api('GET', '/api/users')  // Только в development
```

**⚠️ Рекомендация:** Постепенно заменить все `console.log` на `logger.debug()` или соответствующий метод.

**Текущее состояние:**
- Обнаружено ~147 файлов с `console.log`
- Созданы утилиты для замены
- Замена может быть выполнена постепенно или автоматически перед деплоем

#### 3.2. Переменные окружения
Перед деплоем на Netlify необходимо настроить:

**Обязательные переменные:**
```env
# Database
DATABASE_HOST=your-production-db-host
DATABASE_PORT=3306
DATABASE_USER=your-db-user
DATABASE_PASSWORD=your-secure-password
DATABASE_NAME=atc_production

# JWT Secrets (генерировать: openssl rand -base64 64)
JWT_SECRET=your-very-long-random-secret-key
JWT_EXPIRES_IN=7d
REFRESH_TOKEN_SECRET=your-another-very-long-random-secret-key
REFRESH_TOKEN_EXPIRES_IN=30d

# Telegram Bot
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_WEBHOOK_SECRET=your-webhook-secret

# Application
NODE_ENV=production
AUTO_MIGRATE=true
```

#### 3.3. База данных
- [ ] Настроить внешнюю БД (PlanetScale, Railway, Aiven)
- [ ] Проверить, что миграции применяются корректно
- [ ] Убедиться, что `AUTO_MIGRATE=true` в production

#### 3.4. Telegram Bot
- [ ] Получить токен у @BotFather
- [ ] Настроить webhook URL после деплоя
- [ ] Проверить TELEGRAM_WEBHOOK_SECRET

#### 3.5. Файловое хранилище
- [ ] Настроить постоянное хранилище для `storage/uploads/`
- [ ] Рассмотреть использование S3/Cloudinary для файлов
- [ ] Убедиться, что сертификаты в `server/certs/` доступны

#### 3.6. Безопасность
- [ ] Сменить дефолтный пароль администратора (admin@atc.uz / admin123)
- [ ] Проверить все JWT секреты - они должны быть уникальными и длинными
- [ ] Убедиться, что `.env` не попадает в git
- [ ] Проверить CORS настройки для API

#### 3.7. Производительность
- [ ] Проверить размер бандла: `npm run build`
- [ ] Оптимизировать изображения в `public/`
- [ ] Проверить, что все зависимости в production используются

## 📋 Процесс деплоя на Netlify

### 1. Подготовка репозитория
```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Закоммитить
git commit -m "chore: prepare for production deployment"

# Запушить в основную ветку
git push origin main
```

### 2. Настройка Netlify
1. Зайти в Netlify Dashboard
2. Создать новый сайт из Git репозитория
3. Выбрать репозиторий
4. Настройки сборки:
   - Build command: `npm run build`
   - Publish directory: `dist`
   - Node version: `20`

### 3. Переменные окружения
В Netlify Dashboard → Site settings → Environment variables добавить все переменные из `.env.example`

### 4. Первый деплой
- Netlify автоматически запустит сборку
- Проверить логи сборки
- Убедиться, что миграции применились (AUTO_MIGRATE=true)

### 5. После деплоя
- [ ] Проверить работу приложения
- [ ] Настроить Telegram webhook: `POST /api/telegram/setup-webhook`
- [ ] Сменить пароль администратора
- [ ] Проверить все основные функции

## 🔍 Финальная проверка

### Перед деплоем:
```bash
# Проверка типов
npm run typecheck

# Локальная сборка
npm run build

# Проверка production сборки
npm run preview
```

### После деплоя:
- [ ] Авторизация работает
- [ ] Создание пользователей работает
- [ ] Создание групп работает
- [ ] Календарь отображается
- [ ] Журнал посещаемости работает
- [ ] Telegram бот отвечает
- [ ] Файлы загружаются
- [ ] Сертификаты генерируются
- [ ] Отчеты скачиваются

## 📝 Дополнительные рекомендации

### Мониторинг
- Настроить Netlify Analytics
- Добавить error tracking (Sentry)
- Настроить логирование

### Резервное копирование
- Настроить автоматический бэкап БД
- Сохранять копии загруженных файлов

### Документация
- Создать user guide для администраторов
- Документировать процесс обновления
- Создать FAQ для пользователей

---

**Версия проекта:** 1.0.0  
**Дата подготовки:** 2026-01-17  
**Статус:** Готов к деплою (с учетом выполнения пунктов "Требует внимания")
