# 🚀 Быстрый старт: Google Gemini AI

## ✅ Что уже сделано:

1. ✅ Установлена библиотека `@google/generative-ai`
2. ✅ Создана утилита `server/utils/geminiMatcher.ts`
3. ✅ Интегрирован гибридный поиск в `upload.post.ts`
4. ✅ Добавлена переменная `GEMINI_API_KEY` в `.env`

## 🔑 Что нужно сделать ВАМ:

### Шаг 1: Получить API ключ (2 минуты)

1. Откройте в браузере: **https://aistudio.google.com/apikey**
2. Войдите с вашим Google аккаунтом
3. Нажмите кнопку **"Create API Key"**
4. Выберите **"Create API key in new project"**
5. Скопируйте полученный ключ (выглядит как `AIzaSy...`)

### Шаг 2: Добавить ключ в проект (30 секунд)

1. Откройте файл `.env` в корне проекта
2. Найдите строку:
   ```env
   GEMINI_API_KEY=your_api_key_here
   ```
3. Замените `your_api_key_here` на ваш ключ:
   ```env
   GEMINI_API_KEY=AIzaSy...ваш_настоящий_ключ
   ```
4. Сохраните файл

### Шаг 3: Перезапустить сервер (10 секунд)

```bash
# Остановите текущий сервер (Ctrl+C в терминале)
# Запустите заново:
npm run dev
```

## 🎉 Готово! Теперь протестируйте:

### Тест 1: Загрузите сертификат

1. Откройте http://localhost:3000
2. Загрузите любой сертификат
3. Смотрите в консоль сервера (терминал)

### Что вы увидите:

```
📤 Начало обработки загрузки сертификата
🚀 Запускаем Tesseract OCR...
✅ OCR завершён. Уверенность: 89%
👥 Получение списка сотрудников...
✅ Получено сотрудников: 5

🔍 Шаг 1: Обычный поиск...
⚠️ Обычный поиск не дал результатов

🤖 Шаг 2: Запуск Gemini AI...
✅ Google Gemini API инициализирован
📤 Отправка запроса в Gemini...
⏱️ Gemini ответил за 847мс
📥 Ответ Gemini: {"index": 2, "confidence": 0.95, ...}
✅ AI нашел сотрудника: Ivanov Sergey
🎯 Уверенность AI: 95%
💭 Объяснение AI: Точное совпадение с транслитерацией

🎉 Обработка завершена успешно
```

## 📊 Как понять, что работает?

### Признаки успеха:
- ✅ Видите `🤖 Запуск Gemini AI...`
- ✅ Видите `✅ Google Gemini API инициализирован`
- ✅ Видите `⏱️ Gemini ответил за XXXмс`
- ✅ Сотрудник найден даже с транслитерацией

### Признаки проблем:
- ❌ `GEMINI_API_KEY не настроен` → Проверьте .env
- ❌ `API key not valid` → Ключ неправильный
- ❌ `Quota exceeded` → Превышен лимит (маловероятно)

## 🔧 Устранение проблем

### Проблема: "GEMINI_API_KEY не настроен"
**Решение:** Проверьте, что в `.env` ключ без кавычек:
```env
# ❌ Неправильно:
GEMINI_API_KEY="AIzaSy..."
GEMINI_API_KEY='AIzaSy...'

# ✅ Правильно:
GEMINI_API_KEY=AIzaSy...
```

### Проблема: "API key not valid"
**Решение:** 
1. Проверьте, что скопировали весь ключ
2. Создайте новый ключ на https://aistudio.google.com/apikey
3. Убедитесь, что нет лишних пробелов

### Проблема: AI не запускается
**Решение:**
1. Перезапустите сервер (`Ctrl+C`, затем `npm run dev`)
2. Проверьте консоль на ошибки
3. Убедитесь, что обычный поиск не нашел сотрудника (AI запускается только если обычный поиск не сработал)

## 💡 Полезные команды

```bash
# Проверить, что библиотека установлена:
npm list @google/generative-ai

# Переустановить зависимости:
npm install

# Очистить кэш и переустановить:
rm -rf node_modules package-lock.json
npm install
```

## 📚 Дополнительная информация

- Полная документация: `GEMINI-INTEGRATION.md`
- Код утилиты: `server/utils/geminiMatcher.ts`
- API endpoint: `server/api/certificates/upload.post.ts`

## 🎯 Следующие шаги

После успешного тестирования вы можете:

1. **Настроить порог уверенности** (сейчас 70%):
   ```typescript
   // В upload.post.ts, строка 87:
   if (aiResult.employee && aiResult.confidence > 0.7) {
   //                                            ^^^^ измените на 0.8 или 0.6
   ```

2. **Добавить UI индикатор** метода поиска:
   - Показывать бейдж "Найдено AI" если `matchInfo.method === 'ai'`
   - Показывать уверенность: `matchInfo.confidence`

3. **Собирать статистику**:
   - Сколько раз использовался AI
   - Средняя уверенность
   - Процент успешных находок

---

**Если что-то не работает - напишите мне, разберемся! 🚀**
