# 🚀 Быстрый деплой на Netlify

## Предварительная проверка

```bash
# Проверить готовность проекта
npm run check:deploy

# Проверить типы
npm run typecheck

# Локальная сборка (тест)
npm run build
```

## Шаг 1: Подготовка базы данных

Выберите один из вариантов:

### Вариант A: PlanetScale (Рекомендуется)
1. Зарегистрируйтесь на [planetscale.com](https://planetscale.com)
2. Создайте новую базу данных
3. Получите connection string
4. Скопируйте credentials

### Вариант B: Railway
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте MySQL сервис
3. Получите credentials

### Вариант C: Aiven
1. Зарегистрируйтесь на [aiven.io](https://aiven.io)
2. Создайте MySQL сервис
3. Получите credentials

## Шаг 2: Подготовка Telegram бота

```bash
# 1. Найдите @BotFather в Telegram
# 2. Создайте нового бота: /newbot
# 3. Скопируйте токен
# 4. Сгенерируйте webhook secret:
openssl rand -hex 32
```

## Шаг 3: Генерация JWT секретов

```bash
# JWT Secret
openssl rand -base64 64

# Refresh Token Secret
openssl rand -base64 64
```

## Шаг 4: Деплой на Netlify

### 4.1. Через Netlify Dashboard

1. Зайдите на [netlify.com](https://netlify.com)
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите Git provider (GitHub/GitLab/Bitbucket)
4. Выберите репозиторий
5. Настройки сборки:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
   - **Node version**: `20`

### 4.2. Настройка переменных окружения

В **Site settings** → **Environment variables** добавьте:

```env
# Database
DATABASE_HOST=your-db-host
DATABASE_PORT=3306
DATABASE_USER=your-db-user
DATABASE_PASSWORD=your-db-password
DATABASE_NAME=atc_production

# JWT (используйте сгенерированные секреты!)
JWT_SECRET=your-generated-jwt-secret
JWT_EXPIRES_IN=7d
REFRESH_TOKEN_SECRET=your-generated-refresh-secret
REFRESH_TOKEN_EXPIRES_IN=30d

# Telegram
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_WEBHOOK_SECRET=your-webhook-secret

# Application
NODE_ENV=production
AUTO_MIGRATE=true
```

### 4.3. Запуск деплоя

Netlify автоматически начнет сборку. Следите за логами в разделе **Deploys**.

## Шаг 5: После деплоя

### 5.1. Настройка Telegram Webhook

После успешного деплоя получите URL вашего сайта (например, `https://your-site.netlify.app`)

Выполните POST запрос:

```bash
curl -X POST https://your-site.netlify.app/api/telegram/setup-webhook \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://your-site.netlify.app/api/telegram/webhook"
  }'
```

Или откройте в браузере:
```
https://your-site.netlify.app/api/telegram/setup-webhook
```

### 5.2. Первый вход

1. Откройте ваш сайт
2. Войдите с дефолтными credentials:
   - **Email**: `admin@atc.uz`
   - **Пароль**: `admin123`
3. **ВАЖНО**: Сразу смените пароль!

### 5.3. Проверка функционала

- [ ] Авторизация работает
- [ ] Можно создать пользователя
- [ ] Можно создать группу
- [ ] Календарь отображается
- [ ] Telegram бот отвечает
- [ ] Файлы загружаются

## Шаг 6: Настройка домена (опционально)

1. В Netlify: **Domain settings** → **Add custom domain**
2. Следуйте инструкциям для настройки DNS
3. Netlify автоматически выпустит SSL сертификат

## Обновление проекта

```bash
# 1. Внесите изменения в код
git add .
git commit -m "feat: your changes"
git push origin main

# 2. Netlify автоматически пересоберет и задеплоит
```

## Откат к предыдущей версии

1. В Netlify Dashboard → **Deploys**
2. Найдите нужную версию
3. Нажмите **"Publish deploy"**

## Мониторинг и логи

### Просмотр логов
- **Build logs**: Netlify Dashboard → Deploys → конкретный deploy
- **Function logs**: Netlify Dashboard → Functions → Logs

### Проверка статуса
```bash
# Проверить статус миграций
curl https://your-site.netlify.app/api/health
```

## Troubleshooting

### Ошибка при сборке
1. Проверьте логи сборки в Netlify
2. Убедитесь, что все зависимости установлены
3. Проверьте версию Node.js (должна быть 20)
˜˜YUR
### База данных не подключается
1. Проверьте credentials в Environment variables
2. Убедитесь, что БД доступна извне
3. Проверьте настройки firewall

### Telegram бот не отвечает
1. Проверьте, что webhook настроен: `/api/telegram/webhook`
2. Проверьте токен бота
3. Проверьте логи функций в Netlify

### Миграции не применяются
1. Убедитесь, что `AUTO_MIGRATE=true`
2. Проверьте логи сборки
3. Попробуйте применить миграции вручную через API

## Полезные команды

```bash
# Проверка готовности к деплою
npm run check:deploy

# Локальная сборка
npm run build

# Предпросмотр production сборки
npm run preview

# Проверка типов
npm run typecheck
```

## Поддержка

При возникновении проблем:
1. Проверьте логи в Netlify Dashboard
2. Проверьте переменные окружения
3. Проверьте статус базы данных
4. Обратитесь к [DEPLOYMENT_CHECKLIST.md](DEPLOYMENT_CHECKLIST.md)

---

**Версия**: 1.0.0  
**Последнее обновление**: 2026-01-17
