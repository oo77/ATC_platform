# 🚀 Быстрый деплой на cPanel

## Подготовка (локально)

✅ **Уже выполнено:**

- [x] `npm run build` - проект собран
- [x] Деплой-пакет создан: `deploy-cpanel-20260213-163733.zip` (26.83 MB)

## Шаги на сервере

### 1️⃣ Загрузка файлов (5 мин)

1. Откройте **File Manager** в cPanel
2. Перейдите в `/home/intrauz1/` (или ваш путь)
3. Создайте папку `atc` (если её нет)
4. Загрузите архив `deploy-cpanel-*.zip`
5. Распакуйте архив в папку `atc`
6. Удалите архив (опционально)

**Результат:** Структура `/home/intrauz1/atc/`:

```
atc/
├── .output/
├── .htaccess
├── .env.example
├── package.json
├── package-lock.json
├── storage/
└── tmp/
```

---

### 2️⃣ Создание базы данных (3 мин)

1. Откройте **MySQL Databases** в cPanel
2. **Создайте базу данных:**
   - Имя: `intrauz1_atc_db` (или другое)
   - Нажмите "Create Database"

3. **Создайте пользователя:**
   - Имя: `intrauz1_atc`
   - Пароль: (сгенерируйте надежный, сохраните!)
   - Нажмите "Create User"

4. **Добавьте пользователя к БД:**
   - Выберите пользователя и БД
   - Отметьте "ALL PRIVILEGES"
   - Нажмите "Make Changes"

📝 **Сохраните данные:**

- Host: `localhost`
- Database: `intrauz1_atc_db`
- User: `intrauz1_atc`
- Password: `ваш_пароль`

---

### 3️⃣ Настройка Node.js приложения (5 мин)

1. Откройте **Setup Node.js App** в cPanel

2. **Заполните форму:**

| Поле                         | Значение                          |
| ---------------------------- | --------------------------------- |
| **Node.js version**          | `24.13` (или последняя доступная) |
| **Application mode**         | `Production`                      |
| **Application root**         | `atc`                             |
| **Application URL**          | `intra.uz` (ваш домен)            |
| **Application startup file** | `.output/server/index.mjs`        |

3. Нажмите **"Create"** ✅

---

### 4️⃣ Установка зависимостей (3-5 мин)

В интерфейсе Node.js App:

1. Найдите кнопку **"Run NPM Install"** или **"Установить NPM пакеты"**
2. Нажмите её
3. Дождитесь завершения (2-5 минут)

**Альтернатива через SSH:**

```bash
cd /home/intrauz1/atc
npm install --production
```

---

### 5️⃣ Настройка переменных окружения (10 мин)

В интерфейсе Node.js App нажмите **"Add Environment Variable"** и добавьте:

#### 🔴 Обязательные переменные:

```bash
NODE_ENV=production

# База данных (используйте данные из шага 2)
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USER=intrauz1_atc
DATABASE_PASSWORD=ваш_пароль_из_шага_2
DATABASE_NAME=intrauz1_atc_db

# JWT секреты (сгенерируйте ниже)
JWT_SECRET=ваш_jwt_secret_64_символа
JWT_EXPIRES_IN=7d
REFRESH_TOKEN_SECRET=ваш_refresh_secret_64_символа
REFRESH_TOKEN_EXPIRES_IN=30d

# Автомиграции
AUTO_MIGRATE=true
```

#### 🔵 Опциональные (если используются):

```bash
# Telegram Bot
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_WEBHOOK_SECRET=случайная_строка
TELEGRAM_WEBHOOK_URL=https://intra.uz/api/telegram/webhook

# OpenAI / OpenRouter (для AI-импорта)
OPENAI_API_KEY=ваш_ключ
USE_OPENROUTER=true
OPENAI_VISION_MODEL=openai/gpt-4o
OPENAI_TEXT_MODEL=openai/gpt-3.5-turbo
```

#### 🔑 Генерация JWT секретов:

**Windows PowerShell:**

```powershell
[Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Maximum 256 }))
```

**Linux/Mac/SSH:**

```bash
openssl rand -base64 64
```

Выполните команду **дважды** - для `JWT_SECRET` и `REFRESH_TOKEN_SECRET`

---

### 6️⃣ Установка прав доступа (1 мин)

Через **Terminal** в cPanel или SSH:

```bash
cd /home/intrauz1/atc
chmod -R 755 storage
chmod -R 755 tmp
```

---

### 7️⃣ Запуск приложения (1 мин)

В интерфейсе Node.js App:

1. Нажмите кнопку **"Start App"** или **"Запустить"**
2. Дождитесь статуса **"Running"** (зелёный индикатор)

---

### 8️⃣ Проверка работы (2 мин)

1. **Откройте сайт:** `https://intra.uz`
2. **Должна открыться страница входа**

#### ✅ Если всё работает:

- Страница загружается
- Нет ошибок 500/502
- Можно войти в систему

#### ❌ Если не работает:

**Проверьте логи:**

1. В Node.js App откройте **"Passenger log file"**
2. Ищите ошибки (красным цветом)

**Типичные проблемы:**

| Ошибка                        | Решение                                       |
| ----------------------------- | --------------------------------------------- |
| "Cannot connect to database"  | Проверьте `DATABASE_*` переменные             |
| "Application failed to start" | Проверьте путь к `index.mjs`                  |
| "ENOENT: no such file"        | Создайте недостающие папки в `storage/`       |
| "max_allowed_packet"          | Увеличьте в MySQL или уменьшите размер данных |

---

## 🔄 Перезапуск приложения

После любых изменений:

```bash
touch /home/intrauz1/atc/tmp/restart.txt
```

Или через кнопку **"Restart"** в cPanel Node.js App

---

## 🔄 Обновление приложения

### Способ 1: Только код (быстро)

1. **Локально:**

   ```bash
   npm run build
   ```

2. **На сервере:**
   - Удалите папку `/home/intrauz1/atc/.output`
   - Загрузите новую папку `.output`
   - Перезапустите: `touch tmp/restart.txt`

### Способ 2: Полное обновление

1. Создайте новый деплой-пакет локально
2. Загрузите и распакуйте на сервере (замените файлы)
3. Перезапустите приложение

---

## 📊 Мониторинг

### Просмотр логов

```bash
# Логи Passenger
tail -f /home/intrauz1/atc/logs/passenger.log

# Логи приложения
tail -f /home/intrauz1/atc/logs/app.log
```

### Проверка статуса

В cPanel Node.js App должен быть статус **"Running"** (зелёный)

---

## 🆘 Поддержка

**Подробная документация:** `CPANEL_DEPLOY.md`

**Контрольный чеклист:**

- [ ] Файлы загружены
- [ ] База данных создана
- [ ] Переменные окружения настроены
- [ ] Зависимости установлены
- [ ] Права доступа установлены
- [ ] Приложение запущено
- [ ] Сайт открывается
- [ ] Нет ошибок в логах

---

## ⏱️ Общее время деплоя: ~30 минут

**Разбивка:**

- Загрузка файлов: 5 мин
- Создание БД: 3 мин
- Настройка Node.js App: 5 мин
- Установка зависимостей: 5 мин
- Переменные окружения: 10 мин
- Запуск и проверка: 2 мин

---

**Успешного деплоя! 🚀**
