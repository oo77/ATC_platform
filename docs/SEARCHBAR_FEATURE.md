# Функционал поиска пользователей в SearchBar

## Описание

Реализован функционал глобального поиска пользователей в компоненте `SearchBar.vue`, расположенном в шапке приложения.

## Основные характеристики

### Доступ

- **Доступен только для:** Администраторов (ADMIN) и Модераторов (MANAGER)
- **Скрыт для:** Преподавателей (TEACHER) и Студентов (STUDENT)

### Функциональность поиска

- **Поиск по:** ФИО и ПИНФЛ пользователей
- **Минимальная длина запроса:** 2 символа
- **Максимум результатов:** 4 подсказки
- **Debounce:** 300ms для оптимизации запросов

### Визуальные особенности

- Индикатор загрузки во время поиска
- Автокомплит с выпадающим списком результатов
- Отображение аватара (инициалы пользователя)
- Цветные бейджи ролей:
  - 🔴 ADMIN - красный
  - 🟡 MANAGER - жёлтый
  - 🔵 TEACHER - синий
  - 🟢 STUDENT - зелёный
- Отображение email и ПИНФЛ в результатах
- Кнопка очистки поиска
- Закрытие результатов при клике вне компонента

### Взаимодействие

- Клик по результату → переход на соответствующую страницу:
  - **STUDENT** → `/students/{studentId}` (если есть связь)
  - **TEACHER** → `/instructors/{instructorId}` (если есть связь)
  - **ADMIN/MANAGER** или пользователи без связанных сущностей → `/users/{id}`
- Автоматическое закрытие результатов после выбора
- Сохранение результатов при повторном фокусе

## Техническая реализация

### Backend API

**Endpoint:** `GET /api/users/search`

**Query параметры:**

- `q` (string, required) - поисковый запрос
- `limit` (number, optional) - максимум результатов (по умолчанию 4)

**Пример запроса:**

```
GET /api/users/search?q=Иванов&limit=4
```

**Ответ:**

```json
{
  "success": true,
  "users": [
    {
      "id": "uuid",
      "name": "Иванов Иван Иванович",
      "email": "ivanov@example.com",
      "pinfl": "12345678901234",
      "role": "STUDENT",
      "studentId": "student-uuid",
      ...
    }
  ]
}
```

**Права доступа:**

- Требуется авторизация
- Только ADMIN и MANAGER
- MANAGER не видит пользователей с ролью ADMIN

**Логирование:**

- Все запросы логируются с префиксом `[User Search]`
- Фиксируются попытки несанкционированного доступа

### Frontend компонент

**Файл:** `app/components/layout/header/SearchBar.vue`

**Используемые composables:**

- `useAuth()` - проверка прав доступа
- `useAuthFetch()` - авторизованные запросы

**Основные методы:**

- `handleSearch()` - обработка ввода с debounce
- `performSearch()` - выполнение поискового запроса
- `clearSearch()` - очистка результатов
- `closeResults()` - закрытие выпадающего списка
- `getUserLink()` - определение правильного URL для перехода в зависимости от роли
- `getUserInitials()` - генерация инициалов для аватара
- `getRoleLabel()` - получение русского названия роли
- `getRoleBadgeClass()` - получение CSS классов для бейджа роли

## Безопасность

1. **Проверка на backend:** Двойная проверка прав доступа (middleware + endpoint)
2. **Проверка на frontend:** Компонент скрыт для неавторизованных ролей
3. **SQL Injection защита:** Параметризованные запросы
4. **XSS защита:** Экранирование в шаблонах Vue
5. **Ограничение результатов:** Максимум 4 результата для предотвращения утечки данных

## Производительность

1. **Debounce:** 300ms задержка перед запросом
2. **Минимальная длина:** Поиск только от 2 символов
3. **LIMIT в SQL:** Ограничение на уровне БД
4. **Индексы:** Рекомендуется создать индексы на `name` и `pinfl` в таблице `users`

## Рекомендации по оптимизации БД

Для улучшения производительности поиска рекомендуется создать индексы:

```sql
CREATE INDEX idx_users_name ON users(name);
CREATE INDEX idx_users_pinfl ON users(pinfl);
```

## Возможные улучшения

1. Добавить поиск по email
2. Добавить фильтрацию по ролям
3. Добавить историю поиска
4. Добавить горячие клавиши (Ctrl+K / Cmd+K)
5. Добавить подсветку совпадений в результатах
6. Добавить пагинацию для большего количества результатов
